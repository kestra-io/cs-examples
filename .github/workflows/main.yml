name: Kestra Demo CI/CD
on:
  push:
    branches: [ "demo" ]
  pull_request:
    branches: [ "demo" ]

jobs:
  validate:
    runs-on: ubuntu-latest
    name: Kestra validate
    steps:
      - name: Checkout repo content
        uses: actions/checkout@v4

      - name: Validate all flows
        uses: kestra-io/github-actions/validate-flows@solutions
        with:
          directory: ./
          server: https://partners.kestra.cloud
          apiToken: ${{ secrets.API_KEY }}
  deploy:
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - name: Checkout repository content
        uses: actions/checkout@v4
      - name: Deploy solutions.data flows
        uses: kestra-io/github-actions/deploy-flows@solutions
        with:
          directory: ./solutions.data/flows
          namespace: solutions.data
          server: https://partners.kestra.cloud
          apiToken: ${{ secrets.API_KEY }}

  # Example: Deploy namespace files
  upload_nsfiles:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository content
        uses: actions/checkout@v4
      - name: Upload config YAML to engineering namespace
        uses: kestra-io/github-actions/deploy-namespace-files@solutions
        with:
          localPath: ./solutions.data/files
          namespace: solutions.data
          namespacePath: .
          server: https://partners.kestra.cloud
          apiToken: ${{ secrets.API_KEY }}
